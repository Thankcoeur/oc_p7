pour initialiser le projet taper les commandes
dans le repertoire courant

cd frontend  &&  yarn upgrade && yarn serve

et dans un autre teminal

cd backend && yarn upgrade &&  npm start

le projet et ses dependances sont initialisé